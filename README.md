# Dev Tinder Web

- Create Vite + React application
- Remove unused code and create a Hellow world application
- Installed tailwindcss npm package
- Installed DaisyUI library
- Add NavBar component in App.jsx
- Create NavBar.jsx component for NavBar
- Installed react-roter-dom npm package for routing
- Create BrowserRouter -> Routes -> Route(Body) -> Route Children
- Creat <Outlet /> and <Footer />
- Create login page
- Installed axios package for API call
- Solve cors issue, install corn in backend add cors() middleware with
  configuration origin and credentials: true
- making api call, pass withCredentials: true
- Installed reduxjs/toolkit and react-redux npm package
- Create a configure a Store then create User Slice
- On Login form, making an api call and storing user data in redux store
- NavBar should display login user profile pic and display user first name
- On Logout, cleared the user slice data and redirect to /login page
- You cannot access auth routes without token
- If token is not present, user redirect back to /login page
